import { default as ReactTooltip } from "rc-tooltip";
import "rc-tooltip/assets/bootstrap_white.css";
import "../css/pricing.css";

export const Tooltip = ({ children, summary, details }) => {
  return (

      <div style={{ width: "18rem", cursor: "pointer" }}>
        {summary} &nbsp;
        <ReactTooltip
          overlayStyle={{ inlineSize: "12rem" }}
          placement="right"
          trigger={["hover"]}
          overlay={<span>{details}</span>}
        >
          <span className="admonitionIcon_node_modules-@docusaurus-theme-classic-lib-theme-Admonition-styles-module">
            <svg style={{ width: "1rem", fill: "grey" }} viewBox="0 0 14 16">
              <path
                fill-rule="evenodd"
                d="M6.3 5.69a.942.942 0 0 1-.28-.7c0-.28.09-.52.28-.7.19-.18.42-.28.7-.28.28 0 .52.09.7.28.18.19.28.42.28.7 0 .28-.09.52-.28.7a1 1 0 0 1-.7.3c-.28 0-.52-.11-.7-.3zM8 7.99c-.02-.25-.11-.48-.31-.69-.2-.19-.42-.3-.69-.31H6c-.27.02-.48.13-.69.31-.2.2-.3.44-.31.69h1v3c.02.27.11.5.31.69.2.2.42.31.69.31h1c.27 0 .48-.11.69-.31.2-.19.3-.42.31-.69H8V7.98v.01zM7 2.3c-3.14 0-5.7 2.54-5.7 5.68 0 3.14 2.56 5.7 5.7 5.7s5.7-2.55 5.7-5.7c0-3.15-2.56-5.69-5.7-5.69v.01zM7 .98c3.86 0 7 3.14 7 7s-3.14 7-7 7-7-3.12-7-7 3.14-7 7-7z"
              ></path>
            </svg>
          </span>
        </ReactTooltip>
      </div>

);
};

<br></br>;

<h1 style={{ textAlign: "center" }}>Simple, predictable pricing</h1>
<p style={{ textAlign: "center", marginBottom: "4rem", fontSize: "1.3rem" }}>
  Start building for free, then pay when you scale based on requests and MAUs
</p>

<div style={{ marginBottom: '4rem'}}>

{/* Define pricing data */}
export const pricingData = {
  tiers: [
    { name: 'Starter', price: '$8/mo' },
    { name: 'Pro', price: '$25/mo' },
    { name: 'Enterprise', price: 'Contact us' }
  ],
  features: [
    {
      name: 'Unlimited API proxies',
      included: [true, true, true]
    },
    {
      name: 'Rate limits per user',
      tooltip: 'Prevents abuse with configurable, per-user rate limits (e.g., max 5 OpenAI API calls per user/hour).',
      included: [true, true, true]
    },
    {
      name: 'Resource access control',
      tooltip: 'Sensitive API resources like Files and Threads are protected so only the users that create them can continue to access them.',
      included: [true, true, true]
    },
    {
      name: 'API proxy requests',
      tooltip: 'The sum of requests with a valid JWT, irrespective of status code, made to the API proxy throughout your billing period.',
      values: ['500k included', '2M included, then $1 per 1M', 'Unlimited']
    },
    {
      name: 'Total Users',
      tooltip: 'The maximum number of users making requests to an API proxy.',
      values: ['Unlimited', 'Unlimited', 'Unlimited']
    },
    {
      name: 'MAUs',
      tooltip: 'The sum of distinct users making requests to an API proxy throughout the billing period. Resets every billing cycle.',
      values: ['50k included', '100k included, then $0.003 per MAU', 'Unlimited']
    }
  ]
};

{/* Render pricing table */}
<div style={{ marginBottom: '4rem'}}>
| | {pricingData.tiers.map(tier => `${tier.name}`).join(' | ')} |
|:--|${pricingData.tiers.map(() => ':--:').join('|')}|
| | {pricingData.tiers.map(tier =>
  `<div style={{ fontSize: '1.2rem' }}>${tier.price}</div>`
).join(' | ')} |
{pricingData.features.map(feature => `
| ${feature.tooltip ?
    `<Tooltip summary='${feature.name}' details='${feature.tooltip}'/>` :
    feature.name
  } | ${
    feature.included ?
      feature.included.map(inc => inc ? '&check;' : '').join(' | ') :
      feature.values.join(' | ')
  } |`
).join('\n')}
</div>